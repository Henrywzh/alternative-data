name: OpenRouter Derived Metrics Daily

on:
  schedule:
    - cron: "30 9 * * *"
  workflow_dispatch:

concurrency:
  group: openrouter-derived-metrics-${{ github.ref }}
  cancel-in-progress: false

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Check out repository
        uses: actions/checkout@v7
        with:
          ref: ${{ github.event.repository.default_branch }}
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -e .[dev]

      - name: Synchronize committed inputs
        run: |
          for attempt in 1 2 3; do
            if git pull --rebase origin "${{ github.event.repository.default_branch }}"; then
              exit 0
            fi
            sleep $((attempt * 5))
          done
          exit 1

      - name: Build compact derived marts from committed inputs
        run: openrouter-derived-data --base-dir . build

      - name: Run derived-mart quality tests
        run: |
          python -m pytest -q \
            tests/test_openrouter_derived_data.py \
            tests/test_openrouter_capability_resolver.py

      - name: Commit compact derived marts
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data/normalized/marts/openrouter_usage_economics_daily.parquet \
                  data/normalized/marts/openrouter_workload_intensity_models.parquet
          if git diff --staged --quiet; then
            echo "No OpenRouter derived metric changes to commit"
            exit 0
          fi
          git commit -m "chore: update OpenRouter derived metrics [$(date -u +%Y-%m-%d)]"

          for attempt in 1 2 3; do
            # Rebase before each retry: a bare `git push` cannot recover from
            # a concurrent commit landing on main while this job builds (the
            # 2026-09-02 failure), it just rejects the same non-fast-forward
            # three times and exits 1.  The rebase is conflict-free in
            # practice because only this workflow writes these two marts.
            if git pull --rebase origin "${{ github.event.repository.default_branch }}" && git push; then
              exit 0
            fi
            sleep $((attempt * 5))
          done
          exit 1

      # Deliberately after the commit: a degraded-but-valid mart must still
      # publish. Drift is an alerting concern, not a publication blocker --
      # blocking here is what turned a missing capability entry into days of
      # blank SOTA in the first place.
      - name: Capability drift guard
        id: guard
        continue-on-error: true
        run: |
          openrouter-derived-data --base-dir . guard --top-n 10 --json > guard.json
      - name: Open or update capability-map-drift issue
        if: steps.guard.outcome == 'failure'
        continue-on-error: true
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail
          issue="$(gh issue list --state open --search "capability-map-drift in:title" \
            --json number --jq '.[0].number // empty')"
          if [ -z "$issue" ]; then
            issue="$(gh issue create --title "capability-map-drift" \
              --label "data-quality" \
              --body "The daily capability guard reported unresolved top-10 models. Details follow as comments." \
              | grep -oE '[0-9]+$')"
          fi
          gh issue comment "$issue" \
            --body "$(printf 'Capability guard, %s\n\n```json\n%s\n```\n' "$(date -u +%Y-%m-%d)" "$(cat guard.json)")"
