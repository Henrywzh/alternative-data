{
  "name": "asia-markets-dashboard",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "refresh": "node scripts/run-artifact-builders.mjs",
    "test:refresh-guard": "node --test scripts/artifact-refresh-guard.test.mjs",
    "build": "node scripts/run-artifact-builders.mjs --tolerate-errors && node scripts/build-static-hub.mjs && node scripts/package-dashboard.mjs",
    "dev": "npm run refresh && node scripts/build-static-hub.mjs && python3 -m http.server 4321 --directory dist",
    "preview": "python3 -m http.server 4321 --directory dist",
    "deploy": "npm run build && wrangler pages deploy dist --project-name=asia-markets-dashboard --branch=production --commit-dirty=true"
  },
  "devDependencies": {
    "wrangler": "4.113.0"
  }
}
