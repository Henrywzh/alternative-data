# NOTE: Historically this package collected AWS Spot + Lambda Cloud + OpenRouter catalog.
# AWS Spot and Lambda Cloud collection were removed (see commit history). The package
# name is kept for commit/data-path stability; it now only scrapes the OpenRouter
# model catalog (raw_openrouter_models.parquet).
